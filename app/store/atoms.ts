import { atom } from "jotai";

// Global state for background visibility
export const backgroundVisibleAtom = atom<boolean>(true);

// You can add more global state atoms here as needed
// export const someOtherAtom = atom<string>('');
